{
  "name": "@open-bisbis/ipc",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "biome check src",
    "clean": "echo 'nothing to clean'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@open-bisbis/database": "workspace:*",
    "@open-bisbis/logger": "workspace:*",
    "valibot": "^1.3.1"
  },
  "devDependencies": {
    "electron": "41.5.0",
    "vitest": "^1.6.0"
  }
}
